/**



*/
import * as React from 'react';
import { Form } from '../../forms';
export declare const FormsView: ({ children, }: {
    children?: React.ReactChild | React.ReactChild[];
}) => JSX.Element;
export interface MultiformFormHeaderProps {
    activeForm: Form;
    setActiveFormId(_id: string): void;
    renderNav?: boolean;
}
export declare const MultiformFormHeader: ({ activeForm, setActiveFormId, renderNav, }: MultiformFormHeaderProps) => JSX.Element;
export interface FormHeaderProps {
    activeForm: Form;
    renderNav?: boolean;
}
export declare const FormHeader: ({ renderNav, activeForm }: FormHeaderProps) => JSX.Element;
