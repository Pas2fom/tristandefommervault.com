/**



*/
/// <reference types="react" />
import { Form } from '../../forms';
export interface FormsListProps {
    forms: Form[];
    setActiveFormId(id: string): void;
    isEditing: Boolean;
    hidden?: boolean;
}
export declare const FormList: ({ hidden, forms, setActiveFormId, }: FormsListProps) => JSX.Element;
